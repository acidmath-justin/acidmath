import { MusicGallery } from "@/components/gallery/MusicGallery";

export const metadata = {
  title: "Gallery — Acidmath",
  description: "Campaign photography and the Acidmath playlist.",
};

export default function GalleryPage() {
  return <MusicGallery />;
}
