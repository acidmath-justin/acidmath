import { Manifesto } from "@/components/about/Manifesto";

export const metadata = {
  title: "Manifesto — Acidmath",
  description: "Wearable heresy. Why Acidmath prints on fabric, not paper, and never softens a design for a platform's content policy.",
};

export default function AboutPage() {
  return <Manifesto />;
}
